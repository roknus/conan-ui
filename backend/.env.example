# Conan UI Configuration

# Optional: Custom Conan home directory
# If not specified, uses the system default Conan home
# CONAN_HOME=/path/to/custom/conan/home

# Custom remote configuration (required)
# This is your primary Artifactory/Conan remote
CUSTOM_REMOTE_NAME=your-remote-name
CUSTOM_REMOTE_URL=https://your-artifactory.com/artifactory/api/conan/conan-repo
CUSTOM_REMOTE_USER=your-username
CUSTOM_REMOTE_PASSWORD=your-password

# Optional: Frontend origins allowed for CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
